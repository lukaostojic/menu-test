export enum RoutesMain {
  Login = 'login',
  Dashboard = 'dashboard',
}

export enum RoutesNavigation {
  Currencies = 'currencies',
  Analytics = 'analytics',
}

export enum RoutesCurrencies {
  AddItem = 'add-item',
  EditItem = 'edit-item'
}