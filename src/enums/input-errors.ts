export enum Errors {
  Required = 'This field is required',
  Email = 'Must be a valid email',
  Password = 'Must contain at least 6 characters',
  Code = 'Must have exactly 3 characters',
  Symbol = 'Must be a single character',
  UniqueCode = 'Must be unique'
}